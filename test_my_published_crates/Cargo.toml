[package]
name = "test_my_published_crates"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
thread_local_collect = { version = "0.5", features = ["tlcr"] }
